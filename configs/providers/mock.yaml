name: mock
description: Offline provider for tests and demos.

pricing:
  source_checked_at: "2026-06-03"
  notes: "Offline mock provider; no API cost."

models:
  planner:
    provider: mock
    model: mock-planner
  critic:
    provider: mock
    model: mock-vision-critic
  image:
    provider: mock
    model: mock-image-generator

caveats:
  - "Generates deterministic placeholder images only."
  - "Use this profile for tests and no-key demos."
